class Genome {
    constructor(options) {
        this.speed = options.speed
        this.size = options.size
        this.visionRadius = options.visionRadius

        this.mutationRate = options.mutationRate
    }
    mutate() {}
}