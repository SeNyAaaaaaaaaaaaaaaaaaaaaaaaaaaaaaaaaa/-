class Entity {

    constructor(options) {
        this.id = options.id
        this.x = options.x
        this.y = options.y
        this.size = options.size
        this.color = options.color
    }
    update() {}
    draw() {}
}